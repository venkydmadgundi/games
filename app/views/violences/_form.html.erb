<%= form_with(model: violence, local: true) do |form| %>
  <% if violence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(violence.errors.count, "error") %> prohibited this violence from being saved:</h2>

      <ul>
        <% violence.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
